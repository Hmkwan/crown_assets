# Flask应用配置
SECRET_KEY=your-secret-key-change-this-in-production
FLASK_ENV=development

# PostgreSQL数据库配置
DATABASE_URL=postgresql://postgres:difyai123456@localhost:5432/it_asset

# 可选配置
FALLBACK_ADMIN_EMAIL=admin@example.com

# 企业微信集成 (可选)
WEWORK_CORP_ID=
WEWORK_AGENT_ID=
WEWORK_SECRET=
WEWORK_ENABLED=false

# 如果设置为 1 将跳过 SocketIO 初始化（用于 CI / 脚本 在没有 Redis 的环境下运行）
#SKIP_SOCKETIO_INIT=1  # 已注释以启用 Socket.IO 在容器中（生产环境请核对 Redis 配置）
