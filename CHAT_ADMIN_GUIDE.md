# 聊天管理系统使用指南

## 🎯 功能概述

聊天管理系统为管理员提供全面的聊天数据监控、审计和管理功能。

---

## 📊 访问入口

### 方式一:顶部导航菜单
```
系统设置 → 聊天管理
```
*(仅管理员可见)*

### 方式二:直接访问
```
http://localhost:5020/chat/admin
```

---

## 🔧 功能模块

### 1. 数据概览 (Overview)

#### 统计卡片
显示实时聊天系统关键指标:
- **总会话数** - 系统中所有会话(包括一对一和群组)
- **总消息数** - 所有用户发送的消息总数
- **活跃用户** - 发过至少一条消息的用户数量
- **附件总数** - 上传的文件/图片总数

#### 消息趋势图(折线图)
- 显示最近7天的消息数量变化
- 帮助了解用户活跃度趋势
- 数据自动刷新

#### 会话类型分布(饼图)
- 一对一会话 vs 群组会话比例
- 颜色区分:
  - 蓝色: 一对一会话
  - 红色: 群组会话

---

### 2. 会话管理 (Conversations)

#### 功能特性
- **会话列表** - 显示所有会话及其详细信息
- **类型筛选** - 可按"一对一"或"群组"筛选
- **分页浏览** - 每页显示20条,支持翻页

#### 数据列
| 列名 | 说明 |
|------|------|
| ID | 会话唯一标识 |
| 类型 | 一对一/群组(带标签) |
| 名称 | 会话名称(一对一显示对方用户名) |
| 参与者数 | 当前会话中的用户数量 |
| 消息数 | 该会话的总消息数 |
| 最后消息时间 | 最近一条消息的发送时间 |
| 创建时间 | 会话创建时间 |
| 操作 | 查看/删除按钮 |

#### 操作功能
1. **查看会话**
   - 点击"查看"按钮
   - 跳转到聊天页面并自动选中该会话
   - 可查看完整聊天记录

2. **删除会话**
   - 点击"删除"按钮
   - 二次确认("确定要删除这个会话吗?")
   - ⚠️ **注意**: 删除会话将同时删除所有相关消息,且不可恢复!

---

### 3. 消息审计 (Messages)

#### 功能特性
- **消息列表** - 显示所有用户发送的消息
- **关键词搜索** - 按消息内容搜索
- **分页浏览** - 每页显示50条

#### 数据列
| 列名 | 说明 |
|------|------|
| ID | 消息唯一标识 |
| 发送者 | 发送消息的用户姓名 |
| 消息内容 | 文本内容预览(附件显示"(附件)") |
| 类型 | 文本/图片/文件(带标签) |
| 发送时间 | 消息发送时间 |
| 操作 | 删除按钮 |

#### 搜索功能
1. 在搜索框输入关键词
2. 点击"搜索"按钮或按Enter键
3. 系统将返回包含该关键词的所有消息
4. 清空搜索框可查看所有消息

#### 删除消息
1. 点击消息行的"删除"按钮
2. 二次确认
3. 消息标记为已删除
4. 所有用户的聊天窗口中该消息将消失
5. **用途**: 删除违规内容、敏感信息等

---

### 4. 活跃用户 (Active Users)

#### 功能特性
- **用户排行** - 按消息数量降序排列
- **实时统计** - 显示每个用户的聊天活跃度
- **前100名** - 只显示最活跃的100位用户

#### 数据列
| 列名 | 说明 |
|------|------|
| 排名 | 前3名显示金色徽章 |
| 用户名 | 登录账号 |
| 姓名 | 真实姓名 |
| 部门 | 所属部门 |
| 消息数 | 发送的总消息数(蓝色徽章) |
| 最后消息时间 | 最近一次发消息的时间 |

#### 应用场景
- 了解哪些用户最活跃
- 识别重度使用者
- 评估聊天功能的采用率

---

## 🔐 权限说明

### 管理员权限要求
所有聊天管理功能**仅限管理员**访问:
- 用户角色必须是 `admin`
- 非管理员访问将被重定向到首页并显示错误提示

### API权限验证
所有管理API都包含两层验证:
1. `@login_required` - 必须登录
2. `@admin_required` - 必须是管理员

---

## 📡 API端点

### 统计数据
```
GET /api/chat/admin/statistics
```
返回: 总会话数、消息数、活跃用户、附件数、消息趋势、会话类型分布

### 会话管理
```
GET /api/chat/admin/conversations?page=1&per_page=20&type=direct
DELETE /api/chat/admin/conversations/<id>
```

### 消息审计
```
GET /api/chat/admin/messages?page=1&per_page=50&keyword=关键词
DELETE /api/chat/admin/messages/<id>
```

### 活跃用户
```
GET /api/chat/admin/users/chat-active
```

---

## 💾 数据存储

### 数据库位置
**PostgreSQL数据库**: `it_asset`

### 相关数据表
1. **chat_conversations** - 会话表
2. **chat_participants** - 参与者表
3. **chat_messages** - 消息表
4. **chat_attachments** - 附件表
5. **chat_permissions** - 权限表

### 数据持久化
- 所有聊天数据存储在PostgreSQL中
- Docker重启后数据不会丢失
- 支持标准SQL查询和备份

### Redis使用
- **用途**: WebSocket消息队列(可选)
- **状态**: 当前显示"Redis连接失败,使用内存模式"
- **影响**: 不影响功能,只是容器重启后消息队列会清空
- **建议**: 生产环境可配置Redis持久化

---

## 🎨 界面特性

### 响应式设计
- 支持桌面、平板、手机访问
- 小屏幕自动调整布局

### 视觉元素
- **渐变卡片** - 统计卡片使用渐变色背景
- **动画效果** - 表格行淡入动画
- **徽章标签** - 会话类型、消息类型用彩色徽章标识
- **图标** - 使用Font Awesome图标库

### 图表库
使用 **Chart.js 3.9.1** 绘制:
- 折线图(消息趋势)
- 饼图(会话类型分布)

---

## 🔧 常见问题

### Q1: 为什么看不到"聊天管理"菜单?
**A**: 只有管理员(role='admin')才能看到此菜单。请检查你的用户角色。

### Q2: 删除会话后可以恢复吗?
**A**: 不可以。删除操作是永久性的,会同时删除所有相关消息。请谨慎操作!

### Q3: 消息搜索支持正则表达式吗?
**A**: 当前只支持简单的关键词匹配(SQL LIKE查询),不支持正则表达式。

### Q4: 如何导出聊天数据?
**A**: 当前版本不支持数据导出。可通过数据库管理工具直接查询PostgreSQL。

### Q5: 为什么统计数字不更新?
**A**: 刷新页面即可。未来版本将支持自动刷新。

---

## 🚀 使用场景

### 1. 内容审计
定期检查消息内容,确保没有违规信息:
1. 切换到"消息审计"标签
2. 浏览最近的消息
3. 使用关键词搜索敏感词
4. 删除不当内容

### 2. 系统监控
每日查看统计数据:
1. 查看"数据概览"标签
2. 观察消息趋势图是否正常
3. 检查活跃用户数是否增长

### 3. 问题处理
用户报告会话问题时:
1. 在"会话管理"中找到对应会话
2. 点击"查看"进入聊天页面
3. 检查消息历史
4. 如需删除,点击"删除"按钮

### 4. 用户分析
了解用户使用情况:
1. 切换到"活跃用户"标签
2. 查看排行榜
3. 识别重度用户
4. 评估功能受欢迎程度

---

## 📚 技术架构

### 前端
- **框架**: Bootstrap 4
- **图表**: Chart.js 3.9.1
- **脚本**: chat_admin.js (ES6 Class)
- **样式**: chat_admin.css (渐变、动画)

### 后端
- **框架**: Flask
- **数据库**: PostgreSQL (it_asset)
- **ORM**: SQLAlchemy
- **权限**: Flask-Login + 自定义装饰器

### 实时通信
- **WebSocket**: Flask-SocketIO
- **消息队列**: Redis (可选)

---

## 🛠️ 开发说明

### 文件位置
```
app/
├── templates/
│   └── chat_admin.html         # 管理页面HTML
├── static/
│   ├── css/
│   │   └── chat_admin.css      # 管理页面样式
│   └── js/
│       └── chat_admin.js       # 管理页面逻辑
├── main/
│   └── routes.py               # /chat/admin路由
└── chat_routes.py              # 管理API路由
```

### 添加新功能
1. 在 `chat_routes.py` 添加API端点
2. 在 `chat_admin.js` 添加前端逻辑
3. 在 `chat_admin.html` 添加UI元素
4. 在 `chat_admin.css` 添加样式

---

## ✅ 测试清单

- [ ] 管理员可以访问 `/chat/admin`
- [ ] 非管理员被重定向
- [ ] 统计卡片显示正确数字
- [ ] 消息趋势图正常绘制
- [ ] 会话类型饼图正常绘制
- [ ] 会话列表正常加载
- [ ] 会话类型筛选生效
- [ ] 删除会话功能正常
- [ ] 消息列表正常加载
- [ ] 消息搜索功能正常
- [ ] 删除消息功能正常
- [ ] 活跃用户列表正常显示
- [ ] 分页功能正常

---

**最后更新**: 2025-12-06  
**版本**: v1.0  
**作者**: GitHub Copilot
