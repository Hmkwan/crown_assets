# 维修工单审批流程 - 含金额评估

## 📋 完整流程

### 1. 申请人提交维修工单
- 用户填写设备故障描述
- 选择故障设备
- 提交工单

### 2. 部门负责人初审
**审批内容:**
- 审核故障描述是否合理
- 判断是否需要维修
- 决定: 批准/拒绝

**操作:**
- ✅ 批准 → 进入管理员评估
- ❌ 拒绝 → 工单结束

### 3. 管理员评估维修金额 ⭐ 
**审批内容:**
- 根据故障情况评估维修成本
- 考虑配件费用、人工成本
- **输入预计维修金额**

**操作:**
- 输入金额(必填): 例如 `1500.00`
- 填写评估说明
- ✅ 提交评估

**界面提示:**
```
💰 维修金额评估
作为管理员,请评估此维修工单的预计维修金额。
部门负责人将根据您的评估进行确认。

预计维修金额: ¥ [____] .00
```

### 4. 部门负责人确认金额 ⭐
**审批内容:**
- 查看管理员评估的金额
- 判断金额是否合理
- 决定: 确认/退回重评

**操作选项:**
- ✅ **确认金额合理**: 同意评估,继续流程
- ❌ **金额不符,退回重评**: 退回给管理员重新评估

**退回流程:**
如果选择"退回重评":
1. 填写退回原因(必填)
2. 工单返回管理员
3. 管理员重新评估金额
4. 再次提交给部门负责人确认

**界面显示:**
```
✅ 维修金额确认

管理员评估金额: ¥1,500.00

○ 确认金额合理 - 同意管理员的评估,继续审批流程
○ 金额不符,退回重评 - 要求管理员重新评估维修金额

[如果选择退回]
请说明退回原因:
┌─────────────────────────────────┐
│ 金额偏高,需重新核实配件成本...  │
└─────────────────────────────────┘
```

### 5. 技术员执行维修
**审批内容:**
- 查看批准的维修预算
- 按方案执行维修
- 完成后更新工单状态

**界面显示:**
```
🔧 维修执行
批准的维修预算: ¥1,500.00
请按照审批通过的维修方案执行维修工作
```

## 🔄 循环流程

```
申请人 → 部门负责人(初审) → 管理员(评估金额) → 部门负责人(确认金额)
                                     ↑                        ↓
                                     └────── 退回重评 ────────┘
                                                              ↓
                                                        技术员执行
```

## 💡 金额条件路由(可选)

### 配置节点金额阈值
在工作流节点管理中配置:

**示例配置:**
```
节点: 技术员执行
金额阈值: 5000.00
低于阈值时跳过: 否

节点: 高级审批
金额阈值: 10000.00
低于阈值时跳过: 是  ← 如果金额<10000,跳过此节点
```

### 自动跳过逻辑
系统会自动判断:
- 如果 `repair_cost < amount_threshold` 且 `skip_if_below_threshold = True`
- 则自动跳过该审批节点
- 进入下一个节点

## 📊 审批历史查看

### 普通用户
访问: `/approval_history`
- 查看自己提交的工单审批记录
- 查看需要自己审批的记录
- 按类型、状态筛选

### 管理员
访问: `/approval_history`
- 查看所有审批记录
- 可以干预流程:
  - 强制批准
  - 强制拒绝
  - 转交审批
  - 终止流程

## 🔧 管理员干预

管理员在审批历史页面可以对待审批的工单进行干预:

### 干预操作
1. **强制批准**: 直接批准,跳过所有剩余节点
2. **强制拒绝**: 直接拒绝工单
3. **转交审批**: 将审批转交给其他用户
4. **终止流程**: 终止整个审批流程

### 使用场景
- 紧急情况需要快速通过
- 审批人不在,需要转交
- 工单有误,需要终止

## 📝 数据库字段说明

### RepairOrder (维修工单)
- `repair_cost`: 维修金额 (Decimal 10,2)

### ApprovalWorkflow (审批记录)
- `repair_cost_input`: 审批人输入的金额评估
- `action_type`: 操作类型 (approve/reject/return/transfer)
- `admin_action`: 管理员干预操作
- `transferred_from_id`: 转交来源审批人ID

### WorkflowNode (工作流节点)
- `amount_threshold`: 金额阈值
- `skip_if_below_threshold`: 低于阈值时是否跳过

## 🎯 关键代码位置

### 审批处理逻辑
- 文件: `app/main/routes.py`
- 函数: `approve_repair_order()`
- 增强代码: `approval_enhancements_code.py`

### 审批历史
- 路由: `app/main/approval_history_routes.py`
- 模板: `app/templates/main/approval_history.html`

### 审批表单
- 模板: `app/templates/main/approval_form_with_cost.html`

## 🚀 部署说明

### 1. 运行数据库升级
```bash
python upgrade_approval_system.py
```

### 2. 重启应用
```bash
docker-compose down
docker-compose up -d --build
```

### 3. 测试流程
1. 用普通用户登录,提交维修工单
2. 用部门负责人登录,批准工单
3. 用管理员登录,评估金额
4. 再次用部门负责人登录,确认金额
5. 查看审批历史: `/approval_history`

## ⚠️ 注意事项

1. **金额必填**: 管理员评估时金额必须大于0
2. **退回原因**: 部门负责人退回时必须填写原因
3. **权限控制**: 只有相关角色能看到对应的审批
4. **通知机制**: 每个操作都会发送通知给相关人员
5. **日志记录**: 所有操作都会记录到用户活动日志
