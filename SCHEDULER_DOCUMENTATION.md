# 定时任务自动提醒功能

## 功能概述

系统已实现了自动化的定时任务调度功能,用于管理借用归还流程中的各种提醒和通知。基于APScheduler后台调度器,在系统启动时自动初始化并开始运行。

## 已配置的定时任务

### 1. 检查逾期借用 (check_overdue_loans)

**执行时间**: 每天早上 9:00

**功能描述**:
- 查找所有状态为"借用中"或"已批准"且归还日期已过的借用记录
- 计算逾期天数
- 向借用人发送逾期通知(每次都发送)
- 向管理员发送通知(每3天发送一次,避免通知过多)

**通知内容**:
- **借用人**: "您借用的设备【设备名称】(编号:XXX)已逾期X天,计划归还日期为YYYY-MM-DD,请尽快归还!"
- **管理员**: "设备借用逾期提醒:【设备名称】(编号:XXX)由 用户名 借用,已逾期X天"

### 2. 检查即将到期借用 (check_upcoming_return_dates)

**执行时间**: 每天早上 9:00

**功能描述**:
- 查找3天内即将到期的借用记录
- 向借用人发送到期提醒
- 当天到期的设备会有特别提醒

**通知内容**:
- **当天到期**: "您借用的设备【设备名称】(编号:XXX)今天到期,请及时归还!"
- **即将到期**: "您借用的设备【设备名称】(编号:XXX)将在X天后到期(YYYY-MM-DD),请做好归还准备!"

### 3. 检查待验收归还 (check_pending_return_inspections)

**执行时间**: 每天早上 10:00

**功能描述**:
- 查找状态为"待验收"且申请时间超过2天的归还记录
- 向所有管理员发送处理提醒
- 确保归还验收及时处理

**通知内容**:
- **管理员**: "归还验收提醒:【设备名称】的归还申请已等待X天,请尽快处理!申请人:用户名"

## 技术实现

### 调度器配置

```python
scheduler = BackgroundScheduler(
    timezone='Asia/Shanghai',
    job_defaults={
        'coalesce': True,  # 合并错过的任务
        'max_instances': 1  # 同一任务最多只有1个实例运行
    }
)
```

### 任务注册

使用Cron表达式定义任务执行时间:
```python
# 每天早上9点检查逾期借用
scheduler.add_job(
    func=check_overdue_loans,
    trigger=CronTrigger(hour=9, minute=0),
    id='check_overdue_loans',
    name='检查逾期借用',
    replace_existing=True
)
```

### 日志记录

所有定时任务执行都会记录日志:
- 执行开始时间
- 发现的记录数量
- 发送的通知数量
- 错误信息(如有)

示例日志:
```
[定时任务] 逾期检查完成,发现3条逾期记录,发送5条通知
[定时任务] 即将到期检查完成,发现7条记录,发送7条提醒
[定时任务] 待验收检查完成,无超时待验收
```

## 管理功能

### 查看定时任务状态

管理员可以通过以下路径访问定时任务管理页面:

**导航**: 系统设置 → 定时任务管理

**路由**: `/admin/scheduler/status`

**功能**:
- 查看所有配置的定时任务列表
- 查看任务执行计划和功能描述
- 手动触发任务执行

### 手动触发任务

在定时任务状态页面,管理员可以点击"立即执行"按钮手动触发任务,无需等待到计划执行时间。

**适用场景**:
- 测试功能是否正常
- 紧急情况需要立即检查
- 系统维护后验证

**路由**:
- `/admin/scheduler/trigger/overdue_loans` - 检查逾期借用
- `/admin/scheduler/trigger/upcoming_returns` - 检查即将到期
- `/admin/scheduler/trigger/pending_inspections` - 检查待验收

## 通知系统集成

所有定时任务产生的通知都会:
1. 存储到`Notification`表
2. 显示在用户页面右上角的通知图标中
3. 未读通知会显示红色badge提示数量
4. 用户可以在`/notifications`页面查看所有通知

## 依赖项

- **APScheduler** 3.10.4 - 定时任务调度器
- **pytz** 2023.3 - 时区处理

已添加到`requirements.txt`,在Docker构建时自动安装。

## 启动流程

1. 系统启动时,`create_app()`函数会调用`init_scheduler(app)`
2. 调度器初始化并注册所有定时任务
3. 调度器在后台运行,按照Cron表达式执行任务
4. 每个任务执行时创建新的Flask应用上下文
5. 任务执行完成后记录日志并提交数据库更改

## 错误处理

所有定时任务都包含异常处理:
- 捕获任务执行中的所有异常
- 记录错误日志到应用日志
- 失败时回滚数据库操作
- 不会影响其他任务的执行

## 性能考虑

- **任务合并**: `coalesce=True`确保错过的任务不会重复执行
- **单实例**: `max_instances=1`防止任务并发执行
- **分散执行**: 不同任务设置不同的执行时间(9:00和10:00)
- **管理员通知频率**: 逾期通知每3天发送一次,避免通知过多

## 扩展建议

未来可以添加的定时任务:
1. **设备保养提醒** - 根据保养计划发送提醒
2. **配件库存预警** - 库存低于阈值时通知管理员
3. **维修工单超时提醒** - 维修工单长时间未处理的提醒
4. **报废设备处置提醒** - 已批准报废但未处置的设备提醒
5. **数据库自动备份** - 每天凌晨自动备份数据库
6. **统计报表生成** - 定期生成和发送统计报表

## 测试建议

1. **手动触发测试**: 在定时任务状态页面手动执行各个任务
2. **创建测试数据**: 创建已逾期、即将到期、待验收的借用记录
3. **验证通知**: 检查通知是否正确发送到目标用户
4. **检查日志**: 查看Docker容器日志确认任务执行情况
5. **时间测试**: 等待到计划执行时间验证自动执行

## 故障排查

**问题**: 定时任务没有执行

**排查步骤**:
1. 检查Docker容器日志是否有"[调度器] 定时任务调度器已启动"
2. 确认APScheduler已安装: `docker exec equipment-management-system pip list | grep APScheduler`
3. 查看错误日志: `docker logs equipment-management-system | grep ERROR`
4. 手动触发任务测试功能是否正常

**问题**: 通知未发送

**排查步骤**:
1. 检查Notification表中是否有新记录
2. 确认用户ID是否正确
3. 查看任务执行日志中的发送数量统计
4. 验证数据库查询条件是否正确

## 总结

定时任务自动提醒功能完整实现了借用归还流程的自动化管理,通过及时的通知提醒,提高了设备管理的效率和用户体验。系统管理员可以通过Web界面轻松管理和监控这些任务,确保系统稳定运行。
