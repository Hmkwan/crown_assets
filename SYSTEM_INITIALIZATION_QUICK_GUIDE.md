# 系统初始化 - 快速参考卡

## 🎯 访问入口

**URL:** `http://localhost:5020/admin/database`

需要管理员权限

---

## 📋 操作步骤

### 第 1 步：找到按钮
在数据库管理页面下方"危险操作区域"（深红色警告框）找到：
```
🔄 初始化系统  (红色按钮)
```

### 第 2 步：阅读风险提示
模态框将显示：
- ❌ 将清除的数据列表
- ✅ 将保留的数据列表  
- ⚠️ 操作提示

### 第 3 步：完成三步确认

#### 3a. 输入确认码
```
输入框标签: "步骤 1：输入确认码"
输入内容: INITIALIZE_SYSTEM
```
⚠️ **区分大小写！**

#### 3b. 勾选三个确认项
```
□ 我已备份重要数据
☑️ (必须勾选)

□ 我已知悉所有业务数据将被清除
☑️ (必须勾选)

□ 我同意执行系统初始化操作
☑️ (必须勾选)
```

### 第 4 步：提交
当三个条件全部满足时，"确认初始化"按钮将变为可点击状态
```
点击: [✔️ 确认初始化] 按钮
```

### 第 5 步：最后确认
浏览器会弹出确认对话框：
```
最后确认：您确定要初始化系统吗？
所有业务数据将被清除！

[取消] [确认]
```
点击 `[确认]`

### 第 6 步：等待处理
系统显示进度：
```
🔄 初始化中...
```
处理时间：3-10 秒

### 第 7 步：查看结果
成功提示示例：
```
✅ 系统初始化成功！

📊 初始化统计：
- 删除用户: 15 人
- 删除设备: 42 台
- 备份文件: python_postgresql_backup_20251205_163245.sql.gz

🔐 管理员账号: admin / admin@123

👉 页面将跳转到登录页，请使用新的管理员账号重新登录
```

### 第 8 步：重新登录
```
URL: http://localhost:5020/auth/login
用户名: admin
密码: admin@123
```

---

## 📊 操作效果

### ❌ 清除的数据
- 所有用户账号（除管理员外）
- 所有设备记录
- 所有采购单
- 所有转移/维修/报废记录
- 所有借用记录
- 所有审批申请
- 所有系统日志
- 所有审批备注

### ✅ 保留的数据
- 管理员账号（重置为 admin/admin@123）
- 所有设备类型
- 所有配件类型
- 部门信息
- 地点信息
- 审批流程模板
- 系统角色设置

---

## 🔄 自动备份

操作前系统会自动备份所有数据：
```
文件名: python_postgresql_backup_YYYYMMDD_HHMMSS.sql.gz
位置: backups/ 目录
大小: ~0.15 MB
可恢复: 是
```

备份可通过数据库管理页面的"恢复"功能还原。

---

## ⚠️ 常见问题

### Q：可以取消操作吗？
**A:** 可以。在任何阶段点击 `[取消]` 按钮都会中止操作。

### Q：初始化后可以恢复吗？
**A:** 可以。系统会自动创建备份。在数据库管理页面选择备份文件并点击恢复。

### Q：新管理员密码可以修改吗？
**A:** 可以。登录后进入用户设置，修改密码。

### Q：初始化需要多长时间？
**A:** 通常 3-10 秒，取决于数据量。

### Q：如果初始化失败怎么办？
**A:** 
1. 查看错误信息
2. 检查数据库连接
3. 确保磁盘空间充足
4. 稍后重试

### Q：初始化会影响系统配置吗？
**A:** 不会。所有系统配置、角色权限、工作流等都会保留。

### Q：可以只删除某一类数据吗？
**A:** 不行。初始化是全面清除业务数据，无法按类型选择。

### Q：初始化后数据能再找回吗？
**A:** 只能通过之前的备份恢复。所以必须在初始化前完成三步确认。

---

## 🔑 确认码规则

| 字段 | 要求 |
|------|------|
| 确认码 | 必须输入 `INITIALIZE_SYSTEM` |
| 大小写 | 区分大小写 |
| 空格 | 不能有空格 |
| 确认项 | 必须勾选全部 3 个 |

---

## 📱 界面位置

```
数据库管理页面
├─ 数据库状态卡片 (顶部)
├─ 备份与恢复区域 (左上)
├─ 数据库导出区域 (右上)
├─ 数据库信息 (底部)
└─ 危险操作区域 ⬅️ 这里！
    ├─ 初始化系统 ⬅️ 点击这里
    └─ 重置数据库
```

---

## 🚨 重要提示

⚠️ **此操作无法撤销（除通过备份恢复）**

⚠️ **所有业务数据将被永久删除**

⚠️ **初始化前请确保完整备份**

⚠️ **只有管理员可以执行此操作**

---

## 📞 技术细节

- **API 端点:** `POST /admin/database/initialize`
- **权限要求:** `role == 'admin'`
- **确认参数:** `confirm=INITIALIZE_SYSTEM`
- **返回格式:** JSON
- **备份格式:** PostgreSQL SQL dump + GZIP 压缩
- **备份位置:** `backups/` 目录

---

**最后更新:** 2025-12-05  
**版本:** 1.0  
**适用于:** IT 资产管理系统 v1.0+
