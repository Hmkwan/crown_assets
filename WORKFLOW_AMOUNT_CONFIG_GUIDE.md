# 审批流程金额阈值配置功能

## 功能概述

管理员现在可以通过Web界面自定义审批流程节点的金额阈值和跳过规则,无需修改代码或数据库。

## 访问入口

**导航路径**: 审批菜单 → 金额阈值配置

**URL**: `http://localhost:5020/admin/workflow_config`

**权限要求**: 仅限管理员(admin角色)

---

## 主要功能

### 1. 查看流程配置

- **选择工单类型**: 从下拉菜单选择要配置的工单类型
  - 维修工单
  - 配件申请
  - 设备调拨
  - 设备报废
  - 设备借用
  - 设备申请

- **查看节点列表**: 显示该类型的所有审批节点
  - 序号(执行顺序)
  - 节点名称
  - 所需角色
  - 金额阈值
  - 跳过规则

### 2. 编辑节点配置

点击"编辑"按钮可以修改:

#### 基本信息
- **节点名称**: 例如"管理员审批"、"财务审核"
- **所需角色**: 选择审批该节点的角色
  - 部门负责人
  - 管理员
  - 技术员
  - 仓库管理员
  - 财务
- **执行序号**: 节点在流程中的执行顺序

#### 金额条件
- **金额阈值**: 设置触发金额(单位:元)
  - 例如: 1000.00
  - 留空表示不设置金额条件

- **跳过规则**: 选择以下之一
  1. **金额 < 阈值时跳过**: 低于阈值时自动跳过该节点
  2. **金额 ≥ 阈值时需要**: 达到阈值时才需要该节点

### 3. 添加新节点

点击"添加新节点"按钮创建新的审批节点:

1. 填写节点名称
2. 选择所需角色
3. 设置执行序号
4. (可选)设置金额阈值和跳过规则
5. 点击"添加"保存

### 4. 删除节点

点击"删除"按钮可以移除不需要的审批节点(软删除,可恢复)。

---

## 使用场景示例

### 场景1: 配件申请自动审批

**需求**: 配件金额小于1000元时,无需管理员审批,直接到仓库发货

**配置**:
```
节点: 管理员审批
金额阈值: 1000.00
跳过规则: 金额 < 阈值时跳过
```

**效果**:
- 申请金额 = ¥500 → 跳过管理员审批
- 申请金额 = ¥1500 → 需要管理员审批

### 场景2: 设备报废财务把关

**需求**: 设备价值小于1万元时,无需财务审批

**配置**:
```
节点: 财务审批
金额阈值: 10000.00
跳过规则: 金额 < 阈值时跳过
```

**效果**:
- 设备价值 = ¥5000 → 跳过财务审批
- 设备价值 = ¥15000 → 需要财务审批

### 场景3: 维修金额确认

**需求**: 维修金额达到5000元时,需要部门负责人二次确认

**配置**:
```
节点: 部门负责人确认金额
金额阈值: 5000.00
跳过规则: 金额 ≥ 阈值时需要
```

**效果**:
- 维修金额 = ¥3000 → 跳过二次确认
- 维修金额 = ¥8000 → 需要部门负责人确认

---

## 技术实现

### 数据库字段

**WorkflowNode表**:
- `amount_threshold`: DECIMAL(10,2) - 金额阈值
- `skip_if_below_threshold`: BOOLEAN - 低于阈值时跳过

### API接口

1. **GET /admin/workflow_config**
   - 查看流程配置页面
   - 参数: order_type (工单类型)

2. **POST /admin/workflow_config/update_node/<node_id>**
   - 更新节点配置
   - 请求体: JSON格式配置数据

3. **POST /admin/workflow_config/add_node**
   - 添加新节点
   - 请求体: JSON格式节点数据

4. **POST /admin/workflow_config/delete_node/<node_id>**
   - 删除节点(软删除)

---

## 使用建议

### 最佳实践

1. **合理设置阈值**: 根据实际业务需求设置金额阈值
   - 参考历史审批数据
   - 考虑企业财务制度
   - 定期评估和调整

2. **清晰的节点命名**: 使用描述性名称
   - ✅ "管理员审批(金额≥¥1000)"
   - ❌ "节点2"

3. **保持流程简洁**: 避免过多节点
   - 低价值工单简化流程
   - 高价值工单严格把关

4. **测试后上线**: 修改配置后先测试
   - 创建测试工单验证流程
   - 确认金额阈值正确触发

### 注意事项

⚠️ **修改生效**: 配置修改后立即生效,影响新提交的工单

⚠️ **现有工单**: 不影响已经启动的审批流程

⚠️ **权限控制**: 只有管理员可以修改流程配置

⚠️ **审计追踪**: 建议记录配置变更历史(未来功能)

---

## 界面说明

### 主界面布局

```
┌─────────────────────────────────────────────────┐
│  审批流程配置                                    │
├─────────────────────────────────────────────────┤
│  [选择工单类型: 维修工单 ▼]  [添加新节点]        │
├─────────────────────────────────────────────────┤
│  流程节点配置                                    │
│  ┌─────────────────────────────────────────┐   │
│  │序号│节点名称│角色│金额阈值│跳过规则│操作│   │
│  │ 1  │部门初审│部门│ -     │ -     │编辑│   │
│  │ 2  │管理评估│管理│ -     │ -     │编辑│   │
│  │ 3  │金额确认│部门│¥5000  │≥需要 │编辑│   │
│  │ 4  │技术执行│技术│ -     │ -     │编辑│   │
│  └─────────────────────────────────────────┘   │
├─────────────────────────────────────────────────┤
│  使用说明                                        │
│  [金额阈值设置说明]                              │
│  [示例场景]                                      │
└─────────────────────────────────────────────────┘
```

### 编辑节点对话框

```
┌──────────────────────────────┐
│  编辑流程节点                │
├──────────────────────────────┤
│  节点名称: [____________]    │
│  所需角色: [管理员 ▼]        │
│  执行序号: [2]               │
│  金额阈值: [1000.00]         │
│  跳过规则:                   │
│    ○ 金额 < 阈值时跳过       │
│    ● 金额 ≥ 阈值时需要       │
├──────────────────────────────┤
│         [取消]  [保存]        │
└──────────────────────────────┘
```

---

## 常见问题

### Q1: 修改配置后需要重启系统吗?
**A**: 不需要,配置修改后立即生效。

### Q2: 可以为同一个节点设置多个金额阈值吗?
**A**: 目前每个节点只支持一个金额阈值。如需多级阈值,可以创建多个节点。

### Q3: 如何恢复被删除的节点?
**A**: 联系管理员在数据库中将`is_active`字段设为True,或重新添加节点。

### Q4: 金额阈值的判断逻辑是什么?
**A**: 
- `skip_if_below_threshold = true`: 工单金额 < 阈值时跳过
- `skip_if_below_threshold = false`: 工单金额 ≥ 阈值时需要

### Q5: 不同工单类型的金额来源是什么?
**A**: 
- 维修工单: `repair_cost` (管理员评估的维修金额)
- 配件申请: `SparePart.price × quantity`
- 设备报废: `Equipment.price`
- 其他工单: 根据具体业务逻辑

---

## 未来增强

计划中的功能:

- [ ] 批量编辑节点
- [ ] 导入/导出流程配置
- [ ] 配置变更历史记录
- [ ] 流程模板克隆
- [ ] 可视化流程图编辑
- [ ] 金额阈值区间设置
- [ ] 条件表达式支持(例如: 金额>1000 AND 部门=财务)

---

## 相关文件

**后端路由**: `app/admin/workflow_config_routes.py`

**前端模板**: `app/templates/admin/workflow_config.html`

**数据模型**: `app/models.py` (WorkflowNode, WorkflowTemplate)

**初始化脚本**: `init_default_approval_workflows.py`

---

**最后更新**: 2025-11-30
**版本**: 1.0.0
**作者**: 系统管理员
